#include <stdio.h>

int main(int argc, char const *argv[]) {
  // auto int *heap;
  // heap = (int *)malloc(sizeof(int) * 10);
  // heap = (int *)realloc(heap,sizeof(int) * 100);
  // heap[11] = 10;
  // printf("%d\n", heap[11]);
  // free(heap);
}